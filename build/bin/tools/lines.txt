{
  "tool_name": "lines",
  "description": "Display numer of lines in file",
  "parameters": {
    "type": "object",
    "properties": {
      "file_path": {
        "type": "string",
        "description": "Relative path to folder ('/images')."
      },
    "required": ["file_path"]
  }
}