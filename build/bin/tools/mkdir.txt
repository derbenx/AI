{
  "tool_name": "mkdir",
  "description": "Make Directory",
  "parameters": {
    "type": "object",
    "properties": {
      "file_path": {
        "type": "string",
        "description": "Relative path to dir ('/images')."
      },
    "required": ["file_path"]
  }
}